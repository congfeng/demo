<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cf.code.dao.MsgShareDao">
	
	<insert id="insert" parameterType="com.cf.code.entity.MsgShare">
        INSERT INTO msg_share(ct,user_id,msg_id)
        VALUES (UNIX_TIMESTAMP(NOW()),#{userId},#{msgId})
        <selectKey keyProperty="id" resultType="java.lang.Integer" order="AFTER" statementType="STATEMENT">
            select last_insert_id()
        </selectKey>
    </insert>
    
    <delete id="delete">
        DELETE FROM msg_share WHERE id = #{id,jdbcType=INTEGER}
    </delete>
	
</mapper>