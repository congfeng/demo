<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cf.code.dao.CommunityDao">
	
	<insert id="insert" parameterType="com.cf.code.entity.Community">
		INSERT INTO community(ct,userId,nick,desc,portrait,cover,limitNum,status)
		VALUES (UNIX_TIMESTAMP(NOW()),#{user_id},#{nick},#{desc},#{portrait},#{cover},#{limit_num},#{status})
		<selectKey keyProperty="id" resultType="java.lang.Integer" order="AFTER" statementType="STATEMENT">
			select last_insert_id()
		</selectKey>
	</insert>
	
	<update id="update">
        UPDATE community 
           SET nick = #{nick}
         WHERE id = #{id,jdbcType=INTEGER}
    </update>
	
    <update id="updateStatus">
        UPDATE community
           SET status = #{status}
         WHERE id = #{id,jdbcType=INTEGER}
    </update>
    	
</mapper>