<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cf.code.dao.MusicDao">
    
    <insert id="insert" parameterType="com.cf.code.entity.Music">
        INSERT INTO music(name,author,filename,size,category)
        VALUES (#{name},#{author},#{filename},#{size},#{category})
        <selectKey keyProperty="id" resultType="java.lang.Integer" order="AFTER" statementType="STATEMENT">
            select last_insert_id()
        </selectKey>
    </insert>
    
    <delete id="delete">
        DELETE FROM music WHERE id = #{id,jdbcType=INTEGER}
    </delete>
    
    <update id="updateCollects">
        UPDATE music
           SET collects = #{collects}
         WHERE id = #{id,jdbcType=INTEGER}
    </update>
    
    <update id="update">
        UPDATE music
           SET name = #{name},author = #{author}
         WHERE id = #{id,jdbcType=INTEGER}
    </update>
	
</mapper>